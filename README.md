# LimarenkoIK.github.io

Корневой репозиторий для коротких публичных ссылок и редиректов.

## Папки

- `vpn/` — редирект на импорт VPN-профиля маршрутизации в приложение Happ.  
  URL: <https://limarenkoik.github.io/vpn>

## Управление

Файлы редактируются через `git push` (SSH-ключ `~/.ssh/github_ilya`).
